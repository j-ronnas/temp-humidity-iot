{
  "name": "temp-humid-server",
  "version": "1.0.0",
  "description": "Server to store and process data collected from a temperature and humidity sensor",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "nedb": "^1.8.0"
  }
}
